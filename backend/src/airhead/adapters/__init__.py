"""Calendar source adapters (PRD §8): one protocol, four implementations.

`base` holds the provider-agnostic seam; `contract` the shared behavioral
suite every implementation must pass. Concrete adapters land in M3.
"""
